# Web Framework
fastapi==0.115.6
uvicorn[standard]==0.34.0

# Database
sqlalchemy==2.0.36
asyncpg==0.30.0          # async PostgreSQL driver (FastAPI)
psycopg2-binary==2.9.10  # sync driver for Alembic
alembic==1.14.0

# Validation & Settings
pydantic[email]==2.10.4
pydantic-settings==2.7.0

# Celery client (task dispatch only)
celery==5.4.0
redis==5.2.1

# HTTP & Utilities
httpx==0.28.1
python-dotenv==1.0.1
python-multipart==0.0.20

# Templating (Admin UI)
jinja2==3.1.4
aiofiles==24.1.0

# Active Directory / LDAP (msldap supports NTLM signing required by modern Windows Server)
msldap

# Entra ID / Azure AD SSO (portal authentication)
msal>=1.31.0
itsdangerous>=2.2.0   # required by starlette SessionMiddleware

# Email
aiosmtplib==3.0.2

# License (Ed25519 signature verification)
cryptography>=43.0.0

# Markdown rendering (asset definition help_text → sanitized HTML)
markdown==3.7
bleach==6.2.0

# Prometheus /metrics endpoint
prometheus-client==0.21.0

# OpenTelemetry tracing (HTTP/protobuf exporter avoids grpcio compile pain)
opentelemetry-api==1.29.0
opentelemetry-sdk==1.29.0
opentelemetry-exporter-otlp-proto-http==1.29.0
opentelemetry-instrumentation-fastapi==0.50b0
opentelemetry-instrumentation-sqlalchemy==0.50b0

# Logging
structlog==24.4.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
